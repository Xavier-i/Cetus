#include <armadillo>
#include "Input.h"
#include <string>

using namespace arma;

/*bool ModelInput::LoadFromFile(std::string path, FileType inputType, Object goal) {
  switch (goal) {
  case TARGET: {
    bool result = target.load(path, inputType);
    // check the target data
    return result;
  }
  case DATA:
    return data.load(path, inputType);
  default:
    return false;
  }
}
*/
